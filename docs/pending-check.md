# Pending check fixture

The `Approval gate` job waits for approval from the `fixture-approval` environment.

Leave the deployment unapproved so the pull request keeps a durable pending check without consuming runner time.
